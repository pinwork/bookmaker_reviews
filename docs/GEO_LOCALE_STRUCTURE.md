# 🗺️ Geo-Locale & Regional Data Structure (v1.3)

**Date:** January 18, 2026
**Status:** Production Ready - Unified Object Adaptation Logic

---

## 1. Core Architecture: "Inherit & Transform"

Система використовує **Great Britain (GB/EN)** як глобальний еталон (Master Data). Всі інші регіони не дублюють дані, а успадковують їх через універсальний механізм трансформації об'єктів.

---

## 2. Structural Layers

### 📂 Tier 1: Global Utilities (`/src/data/utils.ts`)

* **Purpose:** Єдиний "процесор" для адаптації даних.
* **Logic:** Функція `adaptObjectToRegion<T>` конвертує будь-яку структуру (Review, Offer, Promo) під локальний ринок, замінюючи валюти та гео-токени на льоту.

### ✍️ Tier 2: Master Content (`/src/data/regions/gb/en/`)

* **Purpose:** Джерело істини для всього контенту.
* **Contents:** Огляди, Бонуси, FAQ, Гайди.
* **Rule:** Будь-які зміни в текстах вносяться **тільки сюди** і автоматично розлітаються по всіх регіонах.

### 🎯 Tier 3: Regional Manifests (`/src/data/regions/{country}/{lang}/`)

* **Purpose:** Локальна активація та технічні параметри.
* **Contents:**
* **`index.ts`:** Маніфест, що визначає, які дані успадковуються, а які перекриваються.
* **`bookmakerData.ts`:** Технічні дані ринку (локальні ліцензії, унікальні афіліатські посилання).



---

## 3. Global Adaptation Engine

Замість ручної заміни полів, система використовує глибоку трансформацію об'єктів:

1. **Currency Mapping:** Автоматична заміна `£` на `€`, `$` тощо згідно з `regionRegistry`.
2. **Geo-Tokenization:** Розумна заміна ключових слів:
* `UK` ➔ `Ireland` / `USA`
* `British` ➔ `Irish` / `American`
* `UKGC` ➔ `Irish Revenue` / `Local Regulator`


3. **Object Integrity:** Зберігаються всі типи даних (числа, дати, булеві значення), змінюється тільки текст всередині рядків.

---

## 4. Fallback Hierarchy & Resolution

При запиті даних для конкретного регіону (напр. `ie`):

1. **Check Local Registry:** Перевірка `enabledBookmakers` у `src/data/regions/index.ts`.
2. **Load Local Overrides:** Завантаження технічних лінків з `ie/en/bookmakerData.ts`.
3. **Inherit & Adapt:** Завантаження контенту з `gb/en/`, пропуск через `adaptObjectToRegion('ie')`.
4. **Final Render:** Збірка об'єкта `BookmakerWithOffer` для фронтенду або Docker-контейнера.

---

## 5. Directory Mapping Example

```text
src/data/
├── utils.ts (Adaptation Brain)
├── regions/
│   ├── index.ts (Global Config)
│   ├── gb/en/ (Master Data)
│   └── ie/en/
│       ├── index.ts (Ireland Manifest)
│       └── bookmakerData.ts (Ireland Licenses/Links)

```

---